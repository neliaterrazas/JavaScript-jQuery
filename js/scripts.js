$(document).ready(function() {
  $("#age").submit(function(event) {
    var age =parseInt($("#yourAge").val());
    if(age <=15) {
      $('#groupOne').show
      $(".")

    }
  }










  });

});
